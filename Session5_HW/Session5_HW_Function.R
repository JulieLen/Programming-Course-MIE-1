#### Session 5 - Homework ####

# Create a function to put into the package

library(roxygen2)

#' A marshallian_demand function
#'
#' This function outpus the the marshallian demand for a linear utility function with two arguments
#' @export
#' @examples
#' marshallian_demand()


marshallian_demand <- function(x, y, px, py, a, b, max){
  U <- aX + bY
  R <- x*px + y*py
  
  returns(mdx, mdy)
}

